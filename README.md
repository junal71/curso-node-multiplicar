# notas:
Este es mi primer programa nodejs
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Base es en numero al que se le calcula la tabla
                                                             [number] [required]
  -l, --listar   decide si se lista o no en pantalla el resultado
                                                      [boolean] [default: false]
  -h, --hasta    Determina hasta que numero se hace la table
                                                          [number] [default: 10]
```